using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Type of destruction to perform different actions when an object must get destroyed
public enum DestroyType { ate, ateByRat, offScreen, grater};

public enum Direction { Left, Right, Up, Down };  